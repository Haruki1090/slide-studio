---
name: slide-studio
description: >
  テーマを与えられて、調査から pptx / pdf 出力までを一続きで行うスライド作成スキル。
  「〇〇についてスライドにして」「資料を作って」「調べてまとめて」「デッキを作って」
  「提案資料」「分析資料」「pptx で」など、**自分で材料を集める必要がある資料作成の依頼は
  すべてこのスキルを使うこと。** 明示的に「スライド」と言われなくても、ある企業・市場・製品・
  テーマを整理して人に見せる成果物を求められたら使う。調査・骨子設計・ボディ設計・実装・
  検証・出力の全工程を、例外処理と品質ゲートを含めて進める。
  材料と構成がすでに手元にあって整形するだけの依頼、単発の図解や 1 枚 HTML、議事録は対象外。
---

# slide-studio

調査を伴うスライド資料を、規則に沿って一続きで作る。

このスキルは**自己完結している。**他のスキルを参照も呼び出しもしない。
必要な検証スクリプトとライブラリはすべて同梱してある。

---

## 0. まず読むもの

| 状況 | 読むファイル |
|---|---|
| 着手時（必ず） | この SKILL.md 全体 |
| 調査に入るとき | `references/research.md` |
| 骨子を立てるとき | `references/narrative.md` |
| テーマを決める・アレンジするとき | `references/design-system.md` |
| 図の種類を選ぶとき | `references/charts.md` |
| 実装するとき | `references/layout.md`、`references/pptxgenjs.md` |
| 検証するとき | `references/qa.md` |
| **想定外のことが起きたとき（頻繁に起きる）** | `references/exceptions.md` |
| 環境の能力を判定するとき | `references/environments.md` |

全部を先に読まない。フェーズに入る直前にそのフェーズのものを読む。

---

## 1. 品質の 7 原則

**以降の全手順はこれに従属する。**手順と原則が衝突したら原則を優先する。
従来の自動生成スライドとの差は、ほぼここに集約される。

| # | 原則 | よくある失敗 |
|---|---|---|
| 01 | **ヘッドメッセージは結論である**。述語を持つ文で書く | 「業績推移」のような体言止めの見出し |
| 02 | **1 枚 1 メッセージ** | 1 枚に論点を 3 つ詰める |
| 03 | **色は意味に固定する**。役割は 3 つだけ | 強調のたびに色が増える |
| 04 | **欠測を隠さない** | 欠けた年を線で結んで滑らかに見せる |
| 05 | **事実と推定を分ける** | 因果を断定して書く |
| 06 | **出典は各ページに置く** | 巻末に一括、または無い |
| 07 | **出力を読み直す**。レイアウト検査と内容の検算は別工程 | コードが通ったら完成とする |

07 の補足。レイアウト検査（重なり・はみ出し）と内容検査（グラフが本文の主張を
支持しているか）は、見る画像が同じでも目的が違う。**混ぜると内容検査が必ず抜ける。**
実際に前回、ヘッドメッセージの事実誤認は内容検査でしか拾えなかった。

---

## 2. 工程とゲート

各フェーズの末尾にゲートがある。**ゲートは「やったか」ではなく「成果物が条件を満たすか」で判定する。**

```
F0 受注解釈   → G0 制約表が埋まっているか／環境の能力を把握したか
F1 調査       → G1 骨子を立てるだけの材料が揃ったか
F2 物語設計   → G2 ガバニングソート 1 文が立ち、各枚が従属するか
                  ★ 全工程で唯一ここだけ止まり、骨子を提示して承認を取る
F3 ボディ設計 → G3 全枚のヘッドメッセージが結論文か／1 枚 1 メッセージか
F4 実装       → G4 形式検証・静的レイアウト検査が通るか
F5 検証       → G5 レイアウト目視 ＋ 数値主張の検算が通るか
F6 納品       → G6 出典・欠測・推定・検証水準の申告が漏れていないか
```

### 回帰（正規の経路。一方通行にしない）

| 回帰 | 発生点 | 戻り先 | 上限 |
|---|---|---|---|
| A 配置の不具合 | G4 / G5 | F4 | 2 周。3 周目に入りそうなら残件を列挙して納品 |
| B 事実誤認 | G5 | F2 または F3 | 上限なし。事実誤認は必ず直す |
| C 材料不足 | G2 / G3 | F1 | 2 回。3 回目は構成側を削る |

---

## 3. 各フェーズの要点

### F0 受注解釈

依頼文から次の 5 項目を埋める。埋まらない項目は既定値を採用し、**採用したことを記録する**。

| 項目 | 既定値 |
|---|---|
| 目的・読み手 | 依頼文から推定。不明なら「対象を把握したい実務者」 |
| テーマ（題材） | 依頼文のまま |
| 枚数 | 可変。下限 6・上限 20 |
| デザインテーマ | G2 で提示して選んでもらう。フォールバックは `default` |
| 出力形式 | G2 で提示。フォールバックは pptx + pdf |

**環境の能力を先に確認する。**

```bash
bash scripts/doctor.sh
```

PDF が出せない環境なら、**着手前にその旨を伝える。**作ってから出せないと言うのが最悪。
詳細は `references/environments.md`。

### F1 調査

`references/research.md` を読んでから始める。要点だけここに書く。

- 広いクエリ（2〜6 語）で地形を掴み、出てきた固有名詞を手がかりに一次情報へ降りる
- **同じクエリを二度投げない。**空振りしたら語を替えるか切り口を変える
- 語を替えて 2 回、切り口を替えて 2 回の計 4 回不発なら**その項目を打ち切る**
- 取得コールの上限は 25 件。超えたら 6 章の劣化戦略へ
- 新旧のソースの**差分**に注意を払う。片方にあって片方に無い事実は、たいてい重要な変化点

### F2 物語設計と承認ゲート

まず**ガバニングソートを 1 文で書く**（主語＋述語、80 字以内）。
これが書けないなら材料が足りないか、構成型が合っていない。`references/narrative.md` の 4 型から選び直す。

次に骨子を組み、**ここで 1 回だけ止まって提示する。**提示するのは 4 つだけ。

1. ガバニングソート 1 文
2. 骨子の表（タイトル／ヘッドメッセージ案／ボディの表現形式）
3. 都度選択の 3 項目を**既定値 1 行で提示**（開いた質問にしない）
4. **作れなかった項目とその理由**

```
テーマ：白地＋墨＋朱／出力：pptx + pdf／Appendix：本編に収める（20 枚超なら分離）
　— このまま進めます。変更があれば指示してください。
```

**例外：**依頼文に完了指示（「出力せよ」等）がある場合、または非対話で応答が得られない場合は、
提示はするが止まらずに続行する。そのとき採用した既定値・食い違い・作れなかった項目は、
**納品時のレポートに必ず書く。**

### F3 ボディ設計

1 枚ずつ、**ボディを先に固めてからヘッドメッセージを書く。**逆順にすると結論が
ボディから出てこなくなる。書けたら、ヘッドメッセージだけを上から拾い読みして
筋が通るか確認する。通らないなら順序か分割が間違っている。

図の選択は `references/charts.md` の「データ形状 → 表現」の表に従う。

### F4 実装

`assets/deck.lib.js` を使う。**座標計算をゼロから書かない。**
生の hex をスライド側に書かない（役割名で指定する。違反すると例外が飛ぶ）。

```js
const { Deck } = require("<skill>/assets/deck.lib.js");
const deck = new Deck({ theme: "default", asOf: "2026-09-12" });
deck.cover({ eyebrow, title, subtitle, kpis });
const s = deck.slide({ title: "業績推移", head: "…結論の文…", source: "…" });
s.label("当期純利益（億円）", M, G.bodyTop, 7.0);
s.bars({ x: M, y: G.bodyTop + 0.38, w: 7.0, h: 3.4, data: [...] });
await deck.save("/mnt/user-data/outputs/<name>.pptx");
```

使えるコンポーネント：`bars` `hbars` `stairs` `cards` `table` `kpis` `timeline`
`callout` `note` `box` `line` `circle` `arrow` `text` `label`。
API と寸法の指針は `references/layout.md`。地雷は `references/pptxgenjs.md`。

`deck.save()` は pptx と同時に `*.manifest.json`（レイアウト台帳）を書く。次の検査で使う。

### F5 検証

**順序を守る。機械で落とせるものを先に落とす。**

```bash
python3 scripts/validate_pptx.py out.pptx            # 形式
python3 scripts/check_layout.py out.manifest.json    # レイアウト（描画前）
bash    scripts/render.sh out.pptx <outdir>          # PDF 4 段梯子 ＋ 画像化
```

`check_layout.py` の ERROR はすべて**生成スクリプトを直して作り直す。**
出力ファイルを手で直さない。WARN は判断のうえ残してよいが、理由を持つこと。

そのうえで**画像を 2 回通す。**

1. **レイアウト目視** — 余白の偏り、視線の流れ、密度
2. **内容の検算** — 本文の数値主張とグラフ・表の値が一致しているか

T2（サブエージェントが使える環境）では 2 を独立したエージェントに任せる。
`agents/reviewer.md` の指示に従い、**画像とチェックリストと主張リストだけを渡す。**
生成スクリプトも設計意図も渡さない。渡すと「そう見えるはず」の補正がかかって、
初見の目という利点が消える。

### F6 納品

`present_files` で pptx と pdf を提示し、**次の 4 つを必ず添える。**

1. 構成のサマリ（枚数と章立て）
2. **作れなかった項目と、その理由**
3. **推定・欠測として扱った箇所**
4. **どの水準で検証したか**（PDF の変換段、目視の有無、未検証の領域）

4 を書かない成果物は、検証していない部分を検証したように見せることになる。

---

## 4. 例外への構え

**想定どおりに進む依頼のほうが少ない。**情報が無い、矛盾する、物語が立たない、
依頼者の前提が事実と違う、といったことは日常的に起きる。

`references/exceptions.md` に 36 項目のカタログがある。各項目は
「検知シグナル／既定の対応／**禁じ手**」で書いてある。禁じ手を必ず読むこと。
放っておくと振る舞いはそちらに流れる。

特に頻度が高く、差が出るもの：

- **一次情報が存在しない** → 代替指標に切り替え、欠けている指標を資料上で明示する。
  業界平均からの逆算を実数のように書かない
- **調査が空振りし続ける** → 4 回で打ち切り、その項目を構成から外し、外したことを申告する
- **材料が足りない** → **枚数を減らして納品する。**一般論や「〇〇とは」で埋めない
- **結論が依頼者の期待と逆** → そのまま出す。ヘッドメッセージで明示し、反証も併記する
- **依頼者の前提が事実と違う** → 調査結果を優先し、**先に食い違いを指摘してから**作る
- **グラフが本文の主張を支持しない** → ヘッドメッセージを直す。**グラフ側を都合よく変えない**

---

## 5. 都度の指定とアレンジ

テーマの差し替えは `assets/themes/*.json` の選択、または `overrides` で部分上書きする。
**レイアウトコードを改変しない。**

```js
new Deck({ theme: "navy", overrides: { grid: { margin: 0.7 } } });
```

同梱テーマ：`default`（白地＋墨＋朱）／`mono`（完全モノクロ）／`navy`（紺基調）／
`editorial`（黒地・大見出し）。

利用者が独自の色を指定してきたら、役割へ写像したうえで検査する。

```bash
python3 scripts/check_contrast.py <theme.json>
```

基準を割った色は**面では使わず、線と文字に限定する。**
色そのものを黙って差し替えない。理由を伝えて運用を変える。
詳細は `references/design-system.md`。

---

## 6. 予算が足りないときの劣化順序

無言で品質を落とすのが最悪なので、**削る順序を決めてある。削ったら申告する。**

```
削ってよい ──────────────────────────────────→ 削ってはいけない
1. 図の表現の多様性（階段図 → 棒グラフに統一）
2. 補助的なスライド（外部環境など背景章）
3. 枚数（13 → 8）
4. 調査の網羅性（周辺論点を落とし中核だけ）
                    ┃ ここから先は削らない
                    ┃ 出典の記載／欠測と推定の注記
                    ┃ ヘッドメッセージの結論性
                    ┃ 数値主張とグラフの一致検証
```

「網羅性を落として中核 8 枚にしました」と言えていれば、劣化ではなく選択になる。

---

## 7. やらないこと

- 社内データや添付ファイルの分析を主とする資料（外部調査との併用は可）
- 30 枚を超える資料。分割を提案する
- アニメーション・画面遷移
- 写真やロゴの配置。**既定では図形とタイポグラフィのみで構成する。**
  出所不明の画像を取得して貼らない。必要なら利用者から素材を受け取る
- 取得を拒否しているページへのブラウザ自動化による回り込み。
  これは能力の制約ではなく規約の制約なので、環境の能力が上がっても越えない
